# CloudPepper Install Guide

Repository root must contain this folder:

```
spx_pharmacy_dispensing/
  __manifest__.py
  models/
  views/
  security/
  reports/
  wizards/
```

Do not upload only the contents of the module folder. CloudPepper must pull the folder itself.

After pulling from GitHub, update apps list and install the module.
